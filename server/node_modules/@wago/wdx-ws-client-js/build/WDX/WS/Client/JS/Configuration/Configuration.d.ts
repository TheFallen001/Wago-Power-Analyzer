export declare enum Protocol {
    ws = "ws",
    wss = "wss"
}
export interface Configuration {
    url?: string;
    protocol?: Protocol;
    host?: string;
    port?: number;
    path?: string;
    reconnectTimeout?: number;
}
