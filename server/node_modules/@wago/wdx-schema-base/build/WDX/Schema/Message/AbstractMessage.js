/**
 * Elrest eDesign Runtime IPC Typescript Messages Abstract
 *
 * @copyright 2024 Elrest AutomationsSysteme GMBH
 */
'use strict';
Object.defineProperty(exports, "__esModule", { value: true });
exports.AbstractMessage = void 0;
const uuid_1 = require("uuid");
class AbstractMessage {
    constructor(body, uuid, error, topic, target, origin, timezone) {
        this.timestamp = Date.now();
        if (body)
            this.body = body;
        this.uuid = uuid !== null && uuid !== void 0 ? uuid : (0, uuid_1.v4)();
        if (error)
            this.error = error;
        this.topic = topic !== null && topic !== void 0 ? topic : undefined;
        if (target)
            this.target = target;
        if (origin)
            this.origin = origin;
        if (timezone)
            this.timezone = timezone;
    }
}
exports.AbstractMessage = AbstractMessage;
//# sourceMappingURL=AbstractMessage.js.map