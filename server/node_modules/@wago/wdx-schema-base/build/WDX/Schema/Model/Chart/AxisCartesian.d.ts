import { Title } from './Title';
import { AxisBounds } from './AxisBounds';
import { AxisPosition } from './AxisPosition';
export declare class AxisCartesian {
    id?: number;
    uuid: string;
    bounds: AxisBounds;
    clip: boolean;
    position: AxisPosition;
    stack: string;
    stackWeight: number;
    offset: boolean;
    title: Title;
}
