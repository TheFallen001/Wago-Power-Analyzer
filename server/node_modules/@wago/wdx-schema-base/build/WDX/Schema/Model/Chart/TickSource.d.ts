export declare enum TickSource {
    /**
     * generates "optimal" ticks based on scale size and time options
     */
    auto = "auto",
    /**
     * generates ticks from data (including labels from data {x|y} objects)
     */
    data = "data",
    /**
     * generates ticks from user given labels ONLY
     */
    labels = "labels"
}
