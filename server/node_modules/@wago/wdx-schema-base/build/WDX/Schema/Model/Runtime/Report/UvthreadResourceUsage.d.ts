/**
 * Elrest eDesign Runtime IPC Typescript Model Runtime
 *
 * @copyright 2024 Elrest Automations Systeme GMBH
 */
export declare class UvthreadResourceUsage {
    userCpuSeconds: number;
    kernelCpuSeconds: number;
    cpuConsumptionPercent: number;
    userCpuConsumptionPercent: number;
    kernelCpuConsumptionPercent: number;
    fsActivity: {
        reads: number;
        writes: number;
    };
    constructor(userCpuSeconds: number, kernelCpuSeconds: number, cpuConsumptionPercent: number, userCpuConsumptionPercent: number, kernelCpuConsumptionPercent: number, fsActivity: {
        reads: number;
        writes: number;
    });
}
