import { Status } from './Status';
import { Author } from './Author';
import { Versions } from './Versions';
export declare class Package {
    id: number;
    author: Array<Author>;
    name: string;
    description: string;
    documentation: string;
    license: string;
    status: Status;
    currentVersion: string;
    newestVersion: string;
    installed: boolean;
    mandatory: boolean;
    versions: Versions;
    constructor(id: number, author?: Array<Author>, name?: string, description?: string, documentation?: string, license?: string, status?: Status, currentVersion?: string, newestVersion?: string, installed?: boolean, mandatory?: boolean, versions?: Versions);
}
