import { ZoomPluginModifierKey } from './ZoomPluginModifierKey';
export declare class ZoomPluginZoomWheel {
    id?: number;
    uuid: string;
    /**
     * Enable zooming via mouse wheel
     */
    enabled: boolean;
    /**
     * Factor of zoom speed via mouse wheel
     */
    speed: number;
    /**
     * Modifier key required for zooming via mouse wheel
     */
    modifierKey: ZoomPluginModifierKey;
}
