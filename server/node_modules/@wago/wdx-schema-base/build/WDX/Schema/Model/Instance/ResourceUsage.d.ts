export declare class ResourceUsage {
    free_memory: number;
    total_memory: number;
    rss: number;
    available_memory: number;
    userCpuSeconds: number;
    kernelCpuSeconds: number;
    cpuConsumptionPercent: number;
    userCpuConsumptionPercent: number;
    kernelCpuConsumptionPercent: number;
    maxRss: number;
    constructor(free_memory: number, total_memory: number, rss: number, available_memory: number, userCpuSeconds: number, kernelCpuSeconds: number, cpuConsumptionPercent: number, userCpuConsumptionPercent: number, kernelCpuConsumptionPercent: number, maxRss: number);
}
